<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ReadBook" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Định nghĩa màu cho thanh Toolbar trong chế độ sáng -->
        <item name="android:toolbarStyle">@style/ToolbarStyle.Light</item>
        <item name="android:statusBarColor">@color/statusBarColor</item>
    </style>

    <style name="Theme.ReadBook" parent="Base.Theme.ReadBook" />
</resources>